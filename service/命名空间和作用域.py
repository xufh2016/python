"""
命名空间（namespace），也叫名称空间或名字空间，是从名字到对象的一个映射关系，类似于字典中的键值对
同一命名空间内不能重名
一共有三种命名空间：
1. 内置名称（built-in names），python语言内置的名称，比如函数名abs、char和异常名称BaseException等等
2. 全局名称（global names），模块中定义的名称，记录了模块的变量，包括函数、类、其他导入的模块、模块级的变量和常量
3. 局部变量（local names），函数中定义的名称，记录了函数的变量，包括函数的参数和局部定义的变量
"""